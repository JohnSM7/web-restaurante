<nav class="nav" id="main-nav" role="navigation" aria-label="Navegación principal">
  <a href="/" class="nav__logo" aria-label="THE EDITORIAL Mediterranean - Inicio">
    THE EDITORIAL
  </a>

  <!-- Desktop links -->
  <ul class="nav__links" id="desktop-links">
    <li><a href="#inicio">Inicio</a></li>
    <li><a href="#nosotros">Nosotros</a></li>
    <li><a href="#carta">Carta</a></li>
    <li><a href="#experiencia">Experiencia</a></li>
    <li><a href="#testimonios">Reseñas</a></li>
    <li><a href="#blog">Blog</a></li>
    <li><a href="#ubicacion">Ubicación</a></li>
  </ul>

  <button class="nav__hamburger" id="hamburger-btn" aria-label="Abrir menú" type="button">
    <span></span>
    <span></span>
    <span></span>
  </button>
</nav>

<!-- Mobile fullscreen menu -->
<div class="nav__mobile-menu" id="mobile-nav" role="dialog" aria-modal="true">
  <a href="#inicio">Inicio</a>
  <a href="#nosotros">Nosotros</a>
  <a href="#carta">Carta</a>
  <a href="#experiencia">Experiencia</a>
  <a href="#testimonios">Reseñas</a>
  <a href="#blog">Blog</a>
  <a href="#ubicacion">Ubicación</a>
  <a href="#reservas" class="nav-mobile-btn">Reservar mesa</a>
</div>

<script is:inline>
  (function() {
    const btn = document.getElementById('hamburger-btn');
    const menu = document.getElementById('mobile-nav');
    
    if (!btn || !menu) return;

    btn.onclick = function() {
      const open = btn.classList.toggle('open');
      menu.classList.toggle('open');
      document.body.style.overflow = open ? 'hidden' : '';
    };

    menu.onclick = function(e) {
      if (e.target.tagName === 'A') {
        btn.classList.remove('open');
        menu.classList.remove('open');
        document.body.style.overflow = '';
      }
    };
  })();
</script>

<style>
  #desktop-links { display: none; }
  @media (min-width: 1024px) {
    #desktop-links { display: flex; }
    .nav__hamburger { display: none !important; }
  }
  
  .nav__hamburger {
    z-index: 200;
    width: 30px;
    height: 30px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 6px;
    position: relative;
  }

  .nav__hamburger span {
    width: 100%;
    height: 1px;
    background: #000;
    transition: 0.3s;
  }

  .nav__hamburger.open span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
  .nav__hamburger.open span:nth-child(2) { opacity: 0; }
  .nav__hamburger.open span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

  .nav__mobile-menu {
    position: fixed;
    inset: 0;
    background: #fff;
    z-index: 150;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    opacity: 0;
    visibility: hidden;
    transition: 0.3s;
  }

  .nav__mobile-menu.open {
    opacity: 1;
    visibility: visible;
  }

  .nav-mobile-btn {
    margin-top: 1rem;
    padding: 1rem 2rem;
    background: #000;
    color: #fff;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
  }
</style>
